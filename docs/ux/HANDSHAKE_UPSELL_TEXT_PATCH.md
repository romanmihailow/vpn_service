# UX-патч: текст первого сообщения после handshake

**Дата:** 2026-03-16  
**Цель:** усилить конверсию trial → paid за счёт более читаемого и продающего текста и короткой CTA-кнопки. Менялись только текст сообщения и текст кнопки; архитектура, follow-up jobs, WG lifecycle, IP pool и платежная логика не трогались.

---

## 1. Старый текст сообщения

```
VPN подключён 👍

Если соединение работает стабильно, можно закрепить доступ, чтобы он не отключился после теста.

Самый популярный тариф:
• 3 месяца — 270 ₽

Это на 30% дешевле помесячной оплаты.

Оформить можно здесь:
/buy
```

---

## 2. Новый текст сообщения

```
VPN подключён 👍

Соединение работает.

Пробный доступ активен 7 дней.

Чтобы VPN не отключился после теста,
можно закрепить доступ уже сейчас.

🔥 Самый популярный тариф
3 месяца — 270 ₽

Это на 30% дешевле помесячной оплаты.
```

Изменения: более короткие фразы, явное напоминание про 7 дней триала, акцент на «закрепить уже сейчас», выделение популярного тарифа эмодзи, убрана ссылка на /buy (действие ведётся через кнопки).

---

## 3. Старый текст первой CTA-кнопки

`💎 Закрепить доступ — 3 месяца / 270 ₽`

---

## 4. Новый текст первой CTA-кнопки

`💎 Закрепить доступ — 270 ₽`

`callback_data` не менялся: `pay:open`. Вторая кнопка «📅 Все тарифы» без изменений.

---

## 5. Изменённые файлы

| Файл | Изменения |
|------|-----------|
| `app/tg_bot_runner.py` | Обновлена константа `HANDSHAKE_USER_CONNECTED_TEXT` и текст первой кнопки в `HANDSHAKE_USER_CONNECTED_KEYBOARD`. |

---

Handshake upsell text optimized.  
CTA button shortened for better readability.

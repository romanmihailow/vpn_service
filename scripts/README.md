# Диагностические скрипты

При проблемах с функциональностью — **сначала проверь этот список** и запусти подходящий скрипт, прежде чем писать новый.

## diagnose_handshake_notification.py

**Назначение:** диагностика уведомлений админу о новых handshake (подписчики, которые впервые подключились к VPN).

**Когда запускать:** админ не получает сообщения «🟢 Новых подписчиков с handshake», хотя в `/ref` растёт «Handshake за 7 дн.» или «подключились».

**Что проверяет:**
- `ADMIN_TELEGRAM_ID`
- количество подписок без уведомления
- сколько из них имеют handshake
- длину сообщения (лимит Telegram 4096 символов)
- первые подписки в батче

**Запуск:**
```bash
python3 scripts/diagnose_handshake_notification.py
```

**Требования:** доступ к БД и WireGuard (`wg show wg0 latest-handshakes`).

**Типичная причина отсутствия уведомлений:** превышение лимита 4096 символов — в коде добавлено разбиение на части.

---

## check_referral_cycles.py

Проверка БД на циклы в реферальном дереве.

---

## delete_admin_referrer.py

Удаление реферера у админа. Запускать один раз при необходимости.

---

## fix_duplicate_ips.py

**Назначение:** устранение дублей IP и применение UNIQUE constraint.

**Действия:**
1. Деактивирует подписки 513 (@rmw_ok) и 380 (tg 646642544)
2. Выдаёт новые IP подпискам 391 и 519
3. Создаёт UNIQUE constraint на (vpn_ip) для активных подписок

**Запуск:**
```bash
python3 scripts/fix_duplicate_ips.py
```

**После скрипта:** отправить конфиг с пояснением и промокодом:
```bash
python3 scripts/send_config_with_promo.py
```
---

## send_config_with_promo.py

Отправка конфига с кастомным caption (объяснение + промокод) списку пользователей.

**Когда использовать:** после fix_duplicate_ips для пользователей 391/519 — отправить им обновлённый конфиг с пояснением и промокодом в подписи к файлу.

**Запуск:** отредактировать USERS и CAPTION в скрипте, затем:
```bash
python3 scripts/send_config_with_promo.py
```
